local module = {}

module.watch = function(Target)
  
end

module.give = function(Target)
  
end

module.inventory = function(Target)
  
end

module.removetool = function(Target)
  
end

setmetatable(module, {__index = function()
  return nil
end})

return module
